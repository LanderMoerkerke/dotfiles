#!/bin/sh

check_updates_packages () {
    # unread=$(find ~/.mail/*/INBOX/new | grep new/ | wc | awk '{print $1;}')
    unread=$(checkupdates | wc -l)

    if [ "$unread" != "0" ]; then
        echo $unread
    else
        echo ""
    fi
}

check_updates_packages
